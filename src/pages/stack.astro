---
import PageLayout from "../layouts/PageLayout.astro";
---

<PageLayout title="Tech Stack" description="Core tools I use to ship reliable AI and full-stack software.">
  <section class="page container" aria-labelledby="stack-title">
    <header class="head">
      <h1 id="stack-title">Tech Stack</h1>
      <p class="sub">From training to deployment to DX.</p>
    </header>

    <div class="groups">
      <article class="group">
        <h3>Core</h3>
        <div class="chips">
          <span class="chip"><span class="iconify" data-icon="logos:python"></span>Python</span>
          <span class="chip"><span class="iconify" data-icon="logos:typescript-icon"></span>TypeScript</span>
          <span class="chip"><span class="iconify" data-icon="logos:pytorch-icon"></span>PyTorch</span>
          <span class="chip"><span class="iconify" data-icon="logos:yolo"></span>YOLOv8</span>
          <span class="chip"><span class="iconify" data-icon="logos:fastapi-icon"></span>FastAPI</span>
          <span class="chip"><span class="iconify" data-icon="logos:react"></span>React</span>
          <span class="chip"><span class="iconify" data-icon="logos:nodejs-icon"></span>Node.js</span>
          <span class="chip"><span class="iconify" data-icon="logos:graphql"></span>GraphQL</span>
        </div>
      </article>

      <article class="group">
        <h3>MLOps & Infra</h3>
        <div class="chips">
          <span class="chip">DVC</span>
          <span class="chip">MLflow</span>
          <span class="chip">BentoML</span>
          <span class="chip"><span class="iconify" data-icon="logos:docker-icon"></span>Docker</span>
          <span class="chip"><span class="iconify" data-icon="logos:kubernetes"></span>Kubernetes</span>
          <span class="chip">GitHub Actions</span>
          <span class="chip">CI Quality Gates</span>
        </div>
      </article>

      <article class="group">
        <h3>Data & Storage</h3>
        <div class="chips">
          <span class="chip"><span class="iconify" data-icon="logos:postgresql"></span>PostgreSQL</span>
          <span class="chip"><span class="iconify" data-icon="logos:mongodb-icon"></span>MongoDB</span>
          <span class="chip"><span class="iconify" data-icon="logos:aws-s3"></span>AWS S3</span>
          <span class="chip">Data Versioning</span>
          <span class="chip">Feature Artifacts</span>
        </div>
      </article>

      <article class="group">
        <h3>Quality & Tooling</h3>
        <div class="chips">
          <span class="chip">Pytest</span>
          <span class="chip">Jest</span>
          <span class="chip">Ruff</span>
          <span class="chip">ESLint</span>
          <span class="chip">Profilers</span>
          <span class="chip">Code Review Automation</span>
        </div>
      </article>
    </div>
  </section>

  <style>
    .page{ padding:2.2rem 1.1rem 3rem; }
    .head h1{ margin:0 0 .4rem; font-size:clamp(1.8rem,3.5vw,2.3rem); letter-spacing:.4px; }
    .head .sub{ margin:0; opacity:.75; }
    .groups{
      margin-top:1.2rem; display:grid; gap:1rem;
      grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
      align-items:stretch;
    }
    .group{
      background:var(--color-bg-alt); border:1px solid var(--color-border);
      border-radius:16px; padding:1rem; display:flex; flex-direction:column; height:100%;
      box-shadow: var(--shadow-sm);
      transition: transform .18s ease, box-shadow .18s ease, border-color .18s ease;
    }
    .group:hover{ transform: translateY(-2px); box-shadow: var(--shadow-md); border-color:var(--color-accent); }
    .group h3{ margin:0 0 .6rem; font-size:1.05rem; letter-spacing:.4px; }
    .chips{ display:flex; flex-wrap:wrap; gap:.5rem; }
    .chip{
      display:inline-flex; align-items:center; gap:.4rem;
      padding:.5rem .8rem; border-radius:9999px; font-weight:800; font-size:.82rem;
      background:var(--color-surface); border:1px solid var(--color-border);
      transition: border-color .18s ease, color .18s ease, transform .18s ease;
    }
    .chip .iconify{ font-size:1rem; }
    .chip:hover{ border-color:var(--color-accent); color: var(--color-accent); transform: translateY(-1px); }
  </style>
</PageLayout>
